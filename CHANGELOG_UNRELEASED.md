### Added

* Added a new parameter `lockCameraToBuilding` in `LibrarySettings` to centers the map on a given building and limits the map zoom and pan to that building bounds.. This parameter only works in iOS right now.
